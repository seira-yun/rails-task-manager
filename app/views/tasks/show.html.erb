<div class="container">
  <div class="d-flex justify-content-between">
    <h1><%= @task.title %></h1>
    <div class="btn btn-primary d-flex align-items-center"><%= link_to "Edit this task", edit_task_path %></div>
  </div>
  <%= link_to "🗑", task_path(@task), method: :delete, data: { confirm: "Really delete #{@task.title}?" } %>
  <p><%= @task.details %></p>
  <% if @task.completed %>
    <p><i class="far fa-check-square me-2"></i>This task is completed</p>
    <% else %>
    <p><i class="far fa-square me-2"></i>This task is not completed</p>
    <% end %>
  <p><%= link_to "Back to list", tasks_path %></p>
</div>
